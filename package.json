{
  "name": "trader-ai",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "postinstall": "chmod +x trader-cli/main.py && mkdir -p node_modules/.bin && ln -sf \"$(pwd)/trader-cli/main.py\" node_modules/.bin/trader && echo '✅ trader CLI linked → node_modules/.bin/trader'"
  },
  "dependencies": {
    "tradingview-mcp": "file:./tradingview-mcp"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "private": true
}
